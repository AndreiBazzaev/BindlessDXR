#include "Model.h"
#include "ResourceManagerImprov.h"
//#include "D3D12HelloTriangle.h"
Model* Model::LoadModel(ResourceManager* resManager, const std::string& name) {

	/*m_name = name;
	if (resManager->GetModel(name) == nullptr) {
		m_app->LoadModelRecursive(m_name, this);
		for (auto& hitGroup : hitGroups) {
			m_hitGroups.push_back(hitGroup);
		}
		resManager->RegisterModel(m_name, this);
		return this;
	}
	else {
		return resManager->GetModel(name);
	}*/
	return nullptr;
}
